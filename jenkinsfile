pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                sh 'mvn -B -DskipTests compile' 
            }
        }
        stage('Package'){
            steps{
                sh 'mvn package'
            }
        }
    }
}
